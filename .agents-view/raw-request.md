# Raw request

Date received: 2026-05-18
Requester: Daryl Walleck (project owner)

## Verbatim ask

> I want you to review the design documents in "C:\Users\dwall\repos\kiro-control-center\Kiro Control Center Design System" it includes the screenshots and designs to implement a create/manage agents view

## Interpreted scope (to be confirmed during interrogation)

A new top-level surface in `kiro-control-center` — the Tauri desktop app — that lets users author and manage Kiro CLI agent definitions stored as JSON files in `.kiro/agents/<name>.json`. Adds a new sidebar group "Workflows" containing a single entry "Agents," with a list page and a 7-section editor (Identity, System Prompt, Tools, MCP Servers, Resources, Hooks, Advanced).

Design references found at:
`Kiro Control Center Design System/design_handoff_agents/` (README, JSON schemas, screenshots, JSX reference source)

## Vague nouns / unresolved decisions in this ask

These are flagged for interrogation, not decided:

- **"create/manage"** — does this include marketplace-installed agents currently tracked in `InstalledAgents`, or only user-authored ones?
- **"agents view"** — does it own the existing `.kiro/agents/<name>.json` directory wholesale, or only files it created?
- **"review the design documents"** — does the user want a literal port of the design, or a port subject to architectural review against existing kiro-control-center patterns?
- **Implicit: this is a frontend-only ask?** — the design adds CRUD over JSON files. The existing codebase has marketplace-write paths but no user-authored-write path. New Tauri commands and core service methods are almost certainly required. Confirm.

## What this raw-request file is for

The skill `interrogated-spec` keeps this file frozen as the starting point. The drift between this file and `spec.md` is the measure of how much was unspecified at the moment of asking. Do not edit this file once interrogation begins.
