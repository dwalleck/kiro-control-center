
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * List all registered marketplaces with plugin counts.
 */
async listMarketplaces() : Promise<Result<MarketplaceInfo[], CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_marketplaces") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List all plugins in a given marketplace.
 */
async listPlugins(marketplace: string) : Promise<Result<PluginInfo[], CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_plugins", { marketplace }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List all available skills for a plugin, cross-referenced with installed state.
 */
async listAvailableSkills(marketplace: string, plugin: string, projectPath: string) : Promise<Result<SkillInfo[], CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_available_skills", { marketplace, plugin, projectPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Install specific skills from a plugin into a Kiro project.
 */
async installSkills(marketplace: string, plugin: string, skills: string[], force: boolean, projectPath: string) : Promise<Result<InstallResult, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("install_skills", { marketplace, plugin, skills, force, projectPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Get summary information about a Kiro project directory.
 */
async getProjectInfo(projectPath: string) : Promise<Result<ProjectInfo, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_project_info", { projectPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List all skills installed in a Kiro project, sorted by name.
 */
async listInstalledSkills(projectPath: string) : Promise<Result<InstalledSkillInfo[], CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_installed_skills", { projectPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove an installed skill from a Kiro project.
 */
async removeSkill(name: string, projectPath: string) : Promise<Result<null, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_skill", { name, projectPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Add a new marketplace source.
 * 
 * Mirrors the CLI `marketplace add` flow:
 * 1. Detect source type (GitHub shorthand, git URL, local path).
 * 2. Clone or symlink into a temp directory inside the cache.
 * 3. Read the marketplace manifest to discover the canonical name.
 * 4. Validate the name, rename to its final location.
 * 5. Register in `known_marketplaces.json`.
 * 6. Return the name and discovered plugins.
 */
async addMarketplace(source: string) : Promise<Result<MarketplaceAddResult, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("add_marketplace", { source }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove a registered marketplace and its cached data.
 */
async removeMarketplace(name: string) : Promise<Result<null, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("remove_marketplace", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Update marketplace clone(s) from remote.
 * 
 * If `name` is provided, only that marketplace is updated. Otherwise all
 * registered marketplaces are updated. Symlinked (local) marketplaces are
 * skipped since they always reflect the latest state on disk.
 */
async updateMarketplace(name: string | null) : Promise<Result<UpdateResult, CommandError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("update_marketplace", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

/**
 * Structured error response for Tauri commands.
 * 
 * Provides a human-readable message alongside a machine-readable error type,
 * enabling the frontend to make type-safe decisions about error handling and
 * display appropriate messages without string parsing.
 */
export type CommandError = { message: string; error_type: ErrorType }
/**
 * Machine-readable error classification for frontend conditional logic.
 * 
 * Serialized as snake_case strings and exported to TypeScript via specta.
 */
export type ErrorType = "not_found" | "already_exists" | "validation" | "git_error" | "io_error" | "parse_error" | "unknown"
/**
 * A skill that failed to install, with the reason.
 */
export type FailedSkill = { name: string; error: string }
/**
 * A marketplace that failed to update, with the reason.
 */
export type FailedUpdate = { name: string; error: string }
/**
 * Result of an install operation across multiple skills.
 */
export type InstallResult = { installed: string[]; skipped: string[]; failed: FailedSkill[] }
/**
 * Information about a single installed skill.
 */
export type InstalledSkillInfo = { name: string; marketplace: string; plugin: string; version: string | null; 
/**
 * ISO 8601 timestamp of when the skill was installed.
 */
installed_at: string }
/**
 * Result of adding a new marketplace, including the discovered plugins.
 */
export type MarketplaceAddResult = { name: string; plugins: PluginBasicInfo[] }
/**
 * Summary information about a registered marketplace.
 */
export type MarketplaceInfo = { name: string; source_type: string; plugin_count: number }
/**
 * Basic information about a plugin within a marketplace.
 */
export type PluginBasicInfo = { name: string; description: string | null }
/**
 * Summary information about a plugin within a marketplace.
 */
export type PluginInfo = { name: string; description: string | null; skill_count: number; source_type: string }
/**
 * Summary information about a Kiro project directory.
 */
export type ProjectInfo = { path: string; kiro_initialized: boolean; installed_skill_count: number }
/**
 * Information about a single skill, including installation status.
 */
export type SkillInfo = { name: string; description: string; plugin: string; marketplace: string; installed: boolean }
/**
 * Result of updating one or more marketplaces.
 */
export type UpdateResult = { updated: string[]; failed: FailedUpdate[]; skipped: string[] }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
