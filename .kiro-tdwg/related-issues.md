# Related issues — kiro-tdwg

Tracker search terms: `remediation`, `RemoteSourceNotLocal`, `dead code`, `error surfacing`, `error projection`.

- **kiro-tdwg** — primary issue. `remediation_hint` was factored out in PR #35 but never wired to any surface.
- **kiro-yhp7** — different domain. Improves orphan-file remediation message UX (F4 from PR #113); same method pattern, unrelated domain.

No duplicate covers the missing surfacing of `remediation_hint` across CLI and Tauri.
